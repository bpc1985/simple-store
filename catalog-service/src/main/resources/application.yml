server.port: 8080
spring:
  application.name: catalog-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/simplestore}
    username: ${SPRING_DATASOURCE_USERNAME:simplestore}
    password: ${SPRING_DATASOURCE_PASSWORD:simplestore}
  jpa:
    hibernate.ddl-auto: update
    show-sql: false
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  cloud.stream:
    function.definition: stockLevelChangedConsumer
    bindings:
      stockLevelChangedConsumer-in-0:
        destination: stock-level-changed
        group: catalog-service
    rabbit.bindings:
      stockLevelChangedConsumer-in-0.consumer:
        exchangeType: fanout
  security.oauth2.resourceserver.jwt:
    issuer-uri: http://identity-service:8080
    jwk-set-uri: http://identity-service:8080/.well-known/jwks.json
jwt.secret: ${JWT_SECRET:c2ltcGxlLXN0b3JlLXNlY3JldC1rZXktZm9yLWp3dC10b2tlbnMtc2hhcmVkLWFjcm9zcy1zZXJ2aWNlcw==}

springdoc:
  api-docs:
    path: /api/v1/catalog/api-docs
  swagger-ui:
    path: /api/v1/catalog/swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  show-actuator: false
